{
  "name": "@lynx-example/web-platform",
  "version": "0.0.0",
  "private": true,
  "description": "An example shows how to use Lynx Web Platform",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm --filter lynx-project run build",
    "dev": "pnpm --filter react-container run dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
  }
}
